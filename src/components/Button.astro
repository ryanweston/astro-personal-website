---
export interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a href={href} class="rounded-full text-sm font-medium px-6 py-2 bg-[var(--foreground)] text-[var(--background)]">
  <slot />
</a>

