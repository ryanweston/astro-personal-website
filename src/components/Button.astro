---
export interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a href={href} class="rounded-full font-medium px-6 py-3 bg-[var(--foreground)] text-[var(--background)]">
  <slot />
</a>

